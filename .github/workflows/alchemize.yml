name: Alchemize

on:
  workflow_call:
    inputs:
      cloudflare-account-id:
        required: true
        type: string
      working-directory:
        required: false
        type: string
        default: .
      environment:
        required: false
        type: string
        default: deploy
    secrets:
      CLOUDFLARE_API_TOKEN:
        required: true
      github-token:
        required: true
      CDP_API_KEY_ID:
        required: false
      CDP_API_KEY_SECRET:
        required: false
      EVM_PROXY_URL:
        required: false
      OTEL_SERVICE_NAME:
        required: false
      SIGNOZ_INGESTION_KEY:
        required: false
      PLANETSCALE_SERVICE_TOKEN:
        required: false

jobs:
  environment:
    runs-on: ubuntu-latest
    outputs:
      stage: ${{ steps.resolve.outputs.stage }}
      pull-request: ${{ steps.resolve.outputs.pull-request }}
    steps:
      - id: resolve
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            echo "stage=pr-${{ github.event.number }}" >> "$GITHUB_OUTPUT"
            echo "pull-request=${{ github.event.number }}" >> "$GITHUB_OUTPUT"
          else
            echo "stage=prod" >> "$GITHUB_OUTPUT"
            echo "pull-request=" >> "$GITHUB_OUTPUT"
          fi

  deploy:
    needs: environment
    if: github.event.action != 'closed'
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    steps:
      - uses: actions/checkout@v6
      - run: "git -c url.https://github.com/.insteadOf=git@github.com: submodule update --init --depth 1 --recursive"
      - uses: ./konfik/.github/actions/configure_environment
      - run: pnpm build
      - name: Deploy
        working-directory: ${{ inputs.working-directory }}
        run: pnpm exec alchemy deploy --stage "$STAGE" --yes
        env:
          STAGE: ${{ needs.environment.outputs.stage }}
          PULL_REQUEST: ${{ needs.environment.outputs.pull-request }}
          CLOUDFLARE_ACCOUNT_ID: ${{ inputs['cloudflare-account-id'] }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          GITHUB_TOKEN: ${{ secrets['github-token'] }}
          CDP_API_KEY_ID: ${{ secrets.CDP_API_KEY_ID }}
          CDP_API_KEY_SECRET: ${{ secrets.CDP_API_KEY_SECRET }}
          EVM_PROXY_URL: ${{ secrets.EVM_PROXY_URL }}
          OTEL_SERVICE_NAME: ${{ secrets.OTEL_SERVICE_NAME }}
          SIGNOZ_INGESTION_KEY: ${{ secrets.SIGNOZ_INGESTION_KEY }}
          PLANETSCALE_SERVICE_TOKEN: ${{ secrets.PLANETSCALE_SERVICE_TOKEN }}

  cleanup:
    needs: environment
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    steps:
      - uses: actions/checkout@v6
      - run: "git -c url.https://github.com/.insteadOf=git@github.com: submodule update --init --depth 1 --recursive"
      - uses: ./konfik/.github/actions/configure_environment
      - name: Destroy
        working-directory: ${{ inputs.working-directory }}
        run: pnpm exec alchemy destroy --stage "$STAGE" --yes
        env:
          STAGE: ${{ needs.environment.outputs.stage }}
          PULL_REQUEST: ${{ needs.environment.outputs.pull-request }}
          CLOUDFLARE_ACCOUNT_ID: ${{ inputs['cloudflare-account-id'] }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          GITHUB_TOKEN: ${{ secrets['github-token'] }}
          CDP_API_KEY_ID: ${{ secrets.CDP_API_KEY_ID }}
          CDP_API_KEY_SECRET: ${{ secrets.CDP_API_KEY_SECRET }}
          EVM_PROXY_URL: ${{ secrets.EVM_PROXY_URL }}
          OTEL_SERVICE_NAME: ${{ secrets.OTEL_SERVICE_NAME }}
          SIGNOZ_INGESTION_KEY: ${{ secrets.SIGNOZ_INGESTION_KEY }}
          PLANETSCALE_SERVICE_TOKEN: ${{ secrets.PLANETSCALE_SERVICE_TOKEN }}
