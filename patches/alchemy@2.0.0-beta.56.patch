diff --git a/lib/Build/Command.d.ts b/lib/Build/Command.d.ts
index ceae677..4917339 100644
--- a/lib/Build/Command.d.ts
+++ b/lib/Build/Command.d.ts
@@ -36,6 +36,10 @@ export interface CommandProps {
      * Environment variables to pass to the build command.
      */
     env?: Record<string, string | Redacted.Redacted<string>>;
+    /**
+     * Run the command on every deploy, even when memoized inputs have not changed.
+     */
+    alwaysRun?: boolean;
 }
 export interface Command extends Resource<"Build.Command", CommandProps, {
     /**
diff --git a/lib/Build/Command.js b/lib/Build/Command.js
index 1c60b53..f6a03db 100644
--- a/lib/Build/Command.js
+++ b/lib/Build/Command.js
@@ -82,6 +82,9 @@ export const CommandProvider = () => Provider.effect(Command, Effect.gen(functio
             if (!output) {
                 return undefined;
             }
+            if (news.alwaysRun) {
+                return { action: "update" };
+            }
             const newHash = yield* hashDirectory(news);
             if (newHash !== output.hash) {
                 return { action: "update" };
@@ -112,7 +115,8 @@ export const CommandProvider = () => Provider.effect(Command, Effect.gen(functio
             const desiredHash = yield* hashDirectory(news);
             const outputPath = getOutputPath(news);
             const cachedExists = output !== undefined && (yield* fs.exists(output.outdir));
-            const reusable = cachedExists &&
+            const reusable = !news.alwaysRun &&
+                cachedExists &&
                 output.hash === desiredHash &&
                 output.outdir === outputPath;
             // Ensure — when the cached artifact is missing, stale, or the
diff --git a/lib/Build/DevServer.js b/lib/Build/DevServer.js
index 23e9c73..4a0b114 100644
--- a/lib/Build/DevServer.js
+++ b/lib/Build/DevServer.js
@@ -113,7 +113,7 @@ import.meta.url.endsWith(".ts") ? "./Local.ts" : "./Local.js", import.meta.url),
     // restart but die when the sidecar shuts down.
     const rootScope = yield* Effect.scope;
     const instances = new Map();
-    const spawn = Effect.fn(function* (props, urlDeferred) {
+    const spawn = Effect.fn(function* (props, urlDeferred, exitDeferred) {
         const [command, ...args] = props.command.split(" ");
         const handle = yield* spawner.spawn(ChildProcess.make(command, args, {
             cwd: props.cwd ?? process.cwd(),
@@ -124,6 +124,7 @@ import.meta.url.endsWith(".ts") ? "./Local.ts" : "./Local.js", import.meta.url),
                     Redacted.isRedacted(v) ? Redacted.value(v) : v,
                 ])),
             },
+            detached: true,
             stdin: "inherit",
             // Leave stdout/stderr piped so we can both mirror to the parent
             // terminal AND parse lines for the first URL.
@@ -132,17 +133,26 @@ import.meta.url.endsWith(".ts") ? "./Local.ts" : "./Local.js", import.meta.url),
         // they're interrupted when the instance scope closes.
         yield* mirrorAndExtract(handle.stdout, "stdout", urlDeferred).pipe(Effect.forkScoped);
         yield* mirrorAndExtract(handle.stderr, "stderr", urlDeferred).pipe(Effect.forkScoped);
+        yield* handle.exitCode.pipe(Effect.flatMap((code) => Deferred.succeed(exitDeferred, code)), Effect.ignore, Effect.forkScoped);
         return handle;
     });
     const stop = Effect.fn(function* (id) {
         const existing = instances.get(id);
         if (existing) {
-            yield* existing.handle.kill();
+            yield* Effect.sync(() => {
+                try {
+                    process.kill(-existing.handle.pid, "SIGTERM");
+                }
+                catch {
+                    // Fall back to killing the direct child below.
+                }
+            });
+            yield* existing.handle.kill().pipe(Effect.ignore);
             yield* Scope.close(existing.scope, Exit.void);
             instances.delete(id);
         }
     });
-    const awaitUrl = (deferred) => Deferred.await(deferred).pipe(Effect.timeoutOption(URL_EXTRACT_TIMEOUT), Effect.map(Option.getOrUndefined));
+    const awaitReady = (urlDeferred, exitDeferred, command) => Effect.raceFirst(Deferred.await(urlDeferred).pipe(Effect.map((url) => ({ url }))), Deferred.await(exitDeferred).pipe(Effect.flatMap((code) => Effect.fail(new Error(`DevServer exited before readiness: ${command} exited with code ${code}`))))).pipe(Effect.timeoutOption(URL_EXTRACT_TIMEOUT), Effect.map(Option.getOrElse(() => ({}))));
     return DevServer.Provider.of({
         diff: Effect.fn(function* ({ id, news }) {
             if (!isResolved(news))
@@ -158,17 +168,16 @@ import.meta.url.endsWith(".ts") ? "./Local.ts" : "./Local.js", import.meta.url),
             const existing = instances.get(id);
             if (existing) {
                 if (existing.hash === hash && (yield* existing.handle.isRunning)) {
-                    const url = yield* awaitUrl(existing.urlDeferred);
-                    return { url };
+                    return yield* awaitReady(existing.urlDeferred, existing.exitDeferred, news.command);
                 }
                 yield* stop(id);
             }
             const scope = yield* Scope.fork(rootScope);
             const urlDeferred = yield* Deferred.make();
-            const handle = yield* spawn(news, urlDeferred).pipe(Scope.provide(scope));
-            instances.set(id, { hash, handle, scope, urlDeferred });
-            const url = yield* awaitUrl(urlDeferred);
-            return { url };
+            const exitDeferred = yield* Deferred.make();
+            const handle = yield* spawn(news, urlDeferred, exitDeferred).pipe(Scope.provide(scope));
+            instances.set(id, { hash, handle, scope, urlDeferred, exitDeferred });
+            return yield* awaitReady(urlDeferred, exitDeferred, news.command);
         }),
         delete: Effect.fn(function* ({ id }) {
             yield* stop(id);
diff --git a/src/Build/Command.ts b/src/Build/Command.ts
index 3c41f7f..f2a104c 100644
--- a/src/Build/Command.ts
+++ b/src/Build/Command.ts
@@ -39,6 +39,10 @@ export interface CommandProps {
    * Environment variables to pass to the build command.
    */
   env?: Record<string, string | Redacted.Redacted<string>>;
+  /**
+   * Run the command on every deploy, even when memoized inputs have not changed.
+   */
+  alwaysRun?: boolean;
 }
 
 export interface Command extends Resource<
@@ -139,6 +143,9 @@ export const CommandProvider = () =>
           if (!output) {
             return undefined;
           }
+          if (news.alwaysRun) {
+            return { action: "update" as const };
+          }
           const newHash = yield* hashDirectory(news);
           if (newHash !== output.hash) {
             return { action: "update" as const };
@@ -171,6 +178,7 @@ export const CommandProvider = () =>
           const cachedExists =
             output !== undefined && (yield* fs.exists(output.outdir));
           const reusable =
+            !news.alwaysRun &&
             cachedExists &&
             output!.hash === desiredHash &&
             output!.outdir === outputPath;
diff --git a/src/Build/DevServer.ts b/src/Build/DevServer.ts
index 21ece39..9a5d229 100644
--- a/src/Build/DevServer.ts
+++ b/src/Build/DevServer.ts
@@ -188,12 +188,14 @@ export const LocalDevServerProvider = () =>
            * appeared at the previous reconcile.
            */
           urlDeferred: Deferred.Deferred<string>;
+          exitDeferred: Deferred.Deferred<number>;
         }
       >();
 
       const spawn = Effect.fn(function* (
         props: DevServerProps,
         urlDeferred: Deferred.Deferred<string>,
+        exitDeferred: Deferred.Deferred<number>,
       ) {
         const [command, ...args] = props.command.split(" ");
 
@@ -209,6 +211,7 @@ export const LocalDevServerProvider = () =>
                 ]),
               ),
             },
+            detached: true,
             stdin: "inherit",
             // Leave stdout/stderr piped so we can both mirror to the parent
             // terminal AND parse lines for the first URL.
@@ -223,6 +226,11 @@ export const LocalDevServerProvider = () =>
         yield* mirrorAndExtract(handle.stderr, "stderr", urlDeferred).pipe(
           Effect.forkScoped,
         );
+        yield* handle.exitCode.pipe(
+          Effect.flatMap((code) => Deferred.succeed(exitDeferred, code)),
+          Effect.ignore,
+          Effect.forkScoped,
+        );
 
         return handle;
       });
@@ -230,16 +238,38 @@ export const LocalDevServerProvider = () =>
       const stop = Effect.fn(function* (id: string) {
         const existing = instances.get(id);
         if (existing) {
-          yield* existing.handle.kill();
+          yield* Effect.sync(() => {
+            try {
+              process.kill(-existing.handle.pid, "SIGTERM");
+            } catch {
+              // Fall back to killing the direct child below.
+            }
+          });
+          yield* existing.handle.kill().pipe(Effect.ignore);
           yield* Scope.close(existing.scope, Exit.void);
           instances.delete(id);
         }
       });
 
-      const awaitUrl = (deferred: Deferred.Deferred<string>) =>
-        Deferred.await(deferred).pipe(
+      const awaitReady = (
+        urlDeferred: Deferred.Deferred<string>,
+        exitDeferred: Deferred.Deferred<number>,
+        command: string,
+      ) =>
+        Effect.raceFirst(
+          Deferred.await(urlDeferred).pipe(Effect.map((url) => ({ url }))),
+          Deferred.await(exitDeferred).pipe(
+            Effect.flatMap((code) =>
+              Effect.fail(
+                new Error(
+                  `DevServer exited before readiness: ${command} exited with code ${code}`,
+                ),
+              ),
+            ),
+          ),
+        ).pipe(
           Effect.timeoutOption(URL_EXTRACT_TIMEOUT),
-          Effect.map(Option.getOrUndefined),
+          Effect.map(Option.getOrElse(() => ({}))),
         );
 
       return DevServer.Provider.of({
@@ -257,8 +287,11 @@ export const LocalDevServerProvider = () =>
 
           if (existing) {
             if (existing.hash === hash && (yield* existing.handle.isRunning)) {
-              const url = yield* awaitUrl(existing.urlDeferred);
-              return { url };
+              return yield* awaitReady(
+                existing.urlDeferred,
+                existing.exitDeferred,
+                news.command,
+              );
             }
 
             yield* stop(id);
@@ -266,12 +299,12 @@ export const LocalDevServerProvider = () =>
 
           const scope = yield* Scope.fork(rootScope);
           const urlDeferred = yield* Deferred.make<string>();
-          const handle = yield* spawn(news, urlDeferred).pipe(
+          const exitDeferred = yield* Deferred.make<number>();
+          const handle = yield* spawn(news, urlDeferred, exitDeferred).pipe(
             Scope.provide(scope),
           );
-          instances.set(id, { hash, handle, scope, urlDeferred });
-          const url = yield* awaitUrl(urlDeferred);
-          return { url };
+          instances.set(id, { hash, handle, scope, urlDeferred, exitDeferred });
+          return yield* awaitReady(urlDeferred, exitDeferred, news.command);
         }),
         delete: Effect.fn(function* ({ id }) {
           yield* stop(id);
